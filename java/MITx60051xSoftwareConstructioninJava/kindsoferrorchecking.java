class kindsoferrorchecking {
    int n = 5;
    //if (n) {
    //    n = n + 1;
    //}
}
/*
EXPLANATION
This is a static type error, because the if statement requires an expression of boolean type, but n has int type.
*/