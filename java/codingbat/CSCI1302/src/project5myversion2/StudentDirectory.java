package project5myversion2;

import java.util.ArrayList;

// another class: StudentDirectory
public class StudentDirectory
{
    // private non-static ArrayList<Student> field, called students.
    private ArrayList<Student> students = new ArrayList<Student>();
 
    
    
    public addStudent(Student student)
    {
        
    }

    /**
     * public String toString() method that returns the list's toString(), i.e., return students.toString();.
     */
    public String toString()
    {
       return students.toString();
    }

    
    /**
     * public void readFromFile(File file) method that uses serialization to read an ArrayList<Student> from a File.
     */
    public void readFromFile(File file)
    {
        
    }
    
    /**
     * public void writeToFile(File file) method that uses serialization to write an ArrayList<Student> to a File.
     */
    public void writeToFile(File file)
    {
        
    }
    
    
}